# YT-Thumbnail-Generator
Layered YouTube thumbnail microservice. FastAPI + Pillow. Composites text, cutouts and hero art as separate passes so typography stays exact and faces never drift. ~200ms per render, no per-image cost.
